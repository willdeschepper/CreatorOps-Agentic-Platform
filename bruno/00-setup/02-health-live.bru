meta {
  name: Health live
  type: http
  seq: 2
}

get {
  url: {{base_url}}/health/live
  body: none
  auth: none
}

tests {
  test("API process is alive", function () {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("ok");
  });
}
