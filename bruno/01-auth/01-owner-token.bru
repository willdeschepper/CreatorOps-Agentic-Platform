meta {
  name: Authenticate owner
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/auth/token
  body: json
  auth: none
}

body:json {
  {
    "email": "{{owner_email}}",
    "password": "{{password}}",
    "brand_slug": "{{brand_slug}}"
  }
}

script:post-response {
  bru.setEnvVar("owner_token", res.body.access_token);
}

tests {
  test("Owner receives a bearer token", function () {
    expect(res.status).to.equal(200);
    expect(res.body.token_type).to.equal("bearer");
  });
}
