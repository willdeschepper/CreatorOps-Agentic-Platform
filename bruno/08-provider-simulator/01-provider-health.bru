meta {
  name: Provider health
  type: http
  seq: 1
}

get {
  url: {{provider_url}}/health
  body: none
  auth: none
}

tests {
  test("Provider simulator is alive", function () {
    expect(res.status).to.equal(200);
    expect(res.body.ok).to.equal(true);
  });
}
