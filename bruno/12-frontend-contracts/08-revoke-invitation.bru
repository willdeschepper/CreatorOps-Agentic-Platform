meta {
  name: Revoke invitation
  type: http
  seq: 8
}

post {
  url: {{base_url}}/v1/invitations/{{revoke_invitation_id}}/revoke
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ops_token}}
}

body:json {
  {
    "comment": "Invitation intentionally revoked in Bruno."
  }
}

tests {
  test("Revoke invitation: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.exist;
  });
}
