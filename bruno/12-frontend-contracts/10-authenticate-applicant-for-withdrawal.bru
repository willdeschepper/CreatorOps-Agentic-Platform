meta {
  name: Authenticate applicant for withdrawal
  type: http
  seq: 10
}

post {
  url: {{base_url}}/v1/auth/token
  body: json
  auth: none
}

body:json {
  {
    "email": "{{withdraw_email}}",
    "password": "{{password}}",
    "brand_slug": null
  }
}

script:post-response {
  bru.setEnvVar("withdraw_creator_token", res.body.access_token);
}

tests {
  test("Authenticate applicant for withdrawal: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.exist;
  });
}
