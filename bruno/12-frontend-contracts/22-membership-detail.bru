meta {
  name: Membership detail
  type: http
  seq: 22
}

get {
  url: {{base_url}}/v1/memberships/{{membership_a_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{creator_a_token}}
}

tests {
  test("Membership detail: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
  });
}
