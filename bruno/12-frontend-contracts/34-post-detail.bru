meta {
  name: Post detail
  type: http
  seq: 34
}

get {
  url: {{base_url}}/v1/posts/{{content_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ops_token}}
}

tests {
  test("Post detail: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
  });
}
