meta {
  name: Finding detail
  type: http
  seq: 37
}

get {
  url: {{base_url}}/v1/agent/findings/{{finding_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{finance_token}}
}

tests {
  test("Finding detail: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
  });
}
