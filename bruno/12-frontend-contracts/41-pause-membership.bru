meta {
  name: Pause membership
  type: http
  seq: 41
}

post {
  url: {{base_url}}/v1/memberships/{{membership_a_id}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ops_token}}
}

body:json {
  {
    "status": "paused",
    "comment": "Bruno verifies membership pause."
  }
}

tests {
  test("Pause membership: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.exist;
  });
}
