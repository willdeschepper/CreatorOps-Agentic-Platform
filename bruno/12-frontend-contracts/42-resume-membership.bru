meta {
  name: Resume membership
  type: http
  seq: 42
}

post {
  url: {{base_url}}/v1/memberships/{{membership_a_id}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ops_token}}
}

body:json {
  {
    "status": "active",
    "comment": "Bruno restores active membership."
  }
}

tests {
  test("Resume membership: HTTP contract", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.exist;
  });
}
